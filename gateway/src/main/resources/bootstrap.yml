spring:
  application:
    name: gateway
  cloud:
    config:
      discovery:
        service-id: config-server
        enabled: true
      fail-fast: true

eureka:
  client:
    service-url:
      default-zone: http://eurekaserver:8761/eureka/